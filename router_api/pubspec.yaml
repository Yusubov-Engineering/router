name: router_api
description: "Api protocols of the router module"
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies: 
  flutter:
    sdk: flutter

dev_dependencies:
  app_linter:
    git:
      url: git@github.com:Yusubov-Engineering/app_linter.git
      path: .
      ref: v1.0.0
